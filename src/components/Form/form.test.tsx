import { beforeEach, describe, expect, it, vi } from 'vitest'
import { mount, VueWrapper, DOMWrapper } from '@vue/test-utils'

// https://github.com/element-plus/element-plus/blob/dev/packages/components/form/__tests__/form.test.tsx
describe('Input', () => {
  it('basic form layout', () => {
    // check how many inputs, labels

  })
  it('click submit should raise errors', () => {
    // form
    // expect(wrapper.find('.el-form-item__error').exists()).toBe(true)
  })
  it('enter correct inputs should not showing any errors', () => {
    // update inputs
  })
  it('reset fields should works fine', () => {

  })
})